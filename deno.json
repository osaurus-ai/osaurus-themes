{
  "tasks": {
    "dev": "deno run --allow-net --allow-env --allow-sys=osRelease,hostname --watch main.ts",
    "start": "deno run --allow-net --allow-env --allow-sys=osRelease,hostname main.ts",
    "test": "deno test --allow-net --allow-env --allow-sys=osRelease,hostname",
    "lint": "deno lint",
    "fmt": "deno fmt",
    "fmt:check": "deno fmt --check",
    "check": "deno check main.ts"
  },
  "imports": {
    "viem": "npm:viem@^2.28.0",
    "ioredis": "npm:ioredis@^5",
    "@aws-sdk/client-s3": "npm:@aws-sdk/client-s3@^3"
  },
  "compilerOptions": {
    "strict": true
  },
  "lint": {
    "rules": {
      "tags": ["recommended"]
    },
    "exclude": ["node_modules/"]
  },
  "fmt": {
    "lineWidth": 100,
    "indentWidth": 2,
    "semiColons": true,
    "singleQuote": false,
    "proseWrap": "preserve",
    "exclude": ["node_modules/"]
  }
}
